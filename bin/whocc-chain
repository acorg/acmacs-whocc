#! /usr/bin/env python3

"""
whocc-chain
"""

from acmacs_py import run, Path, chain202105

# ======================================================================

def main(args):
    chain202105.run(args.chain_dir)
    # with email.send_after():
    #     settings = setup(args)


# ----------------------------------------------------------------------

run(main, {
    "chain_dir": {"type": Path},
    # ("-v", "--verbose"): {"action": "store_true"},
    })

# ======================================================================
### Local Variables:
### eval: (if (fboundp 'eu-rename-buffer) (eu-rename-buffer))
### End:
